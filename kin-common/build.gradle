apply plugin: 'java'
apply plugin: 'eclipse'

// sourceCompatibility = 10

jar {
  archiveName = 'kin-common.jar'
  manifest {
    attributes 'Implementation-Title': 'kin-common',
               'Implementation-Version': version
  }
}

repositories {
  mavenCentral()
}

uploadArchives {
  repositories {
     flatDir {
       dirs 'repos'
     }
  }
}
